@AGENTS.md

## Claude-specific notes
- Run tests through the Bash/PowerShell tool by invoking the virtual environment Python directly (e.g. `./envsubtrans/Scripts/python.exe tests/unit_tests.py`). No shell activation step is needed.
